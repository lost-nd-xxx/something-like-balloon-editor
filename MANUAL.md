# Something Like Balloon Editor 操作マニュアル

---

## 使い方

### 1. プロジェクトを用意する

このアプリはバルーン素材を「プロジェクト」として管理します。
プロジェクトとは `projects/（プロジェクト名）/` フォルダのことで、descript.txt・install.txt・画像ファイルなどが含まれます。

プロジェクトを用意する方法は3つあります。

#### 新規プロジェクトを作成する

メニューバーの「ファイル > 新規プロジェクト...」からプロジェクト名を入力して作成します。
空の descript.txt / install.txt が生成されます。

#### 既存フォルダからプロジェクトを作成する

メニューバーの「ファイル > フォルダからプロジェクトを作成...」でフォルダを選択します。
選択フォルダ内の PNG / PNA / PNR / TXT ファイルが新しいプロジェクトフォルダにコピーされます。

- Shift_JIS など UTF-8 以外のテキストファイルは自動的に UTF-8 に変換され、`descript.txt` / `install.txt` の `charset,` 宣言も UTF-8 に統一されます（宣言が無い場合は補完されます）。
- `descript.txt` に選択肢（cursor）・アンカー（anchor）の装飾設定がグループ単位で含まれていない場合、アプリの初期値が補完されます。補完が行われた場合は作成後に通知が表示されます。

取り込み元のフォルダ自体は変更されません（コピー先のプロジェクトフォルダのみ変換・補完されます）。

#### 既存プロジェクトを開く

メニューバーの「ファイル > プロジェクトを開く...」でプロジェクト一覧から選択して開きます。

プロジェクトを開いた際、`descript.txt` / `install.txt` の `charset,` 宣言が UTF-8 以外だった場合は自動的に UTF-8 へ修正され、通知が表示されます（ファイルの中身がそもそも UTF-8 として読み込めない場合は修正されず、警告のみ表示されます）。装飾設定の補完も同様にこのタイミングで行われます。

---

### 2. バルーン画像の配色を設定する

上部ツールバーのカラーピッカーでレイヤーカラーを指定します。

| レイヤー | 対象 |
|----------|------|
| 塗り | バルーン本体の背景色 |
| 縁取り | バルーンの縁・枠線 |
| 題字 | インプットボックスのテキスト（balloonc系のみ） |
| 部品 | 矢印・クリック待ち等のパーツ類 |

- カラーボタンをクリックするとカラーダイアログが開きます。
- テキストボックスに `#RRGGBB` 形式で直接入力してEnterキーを押すこともできます。
- **「色を一括適用」**: 有効にすると全バルーンに同じ配色を適用します。無効のときは選択中バルーンのみに適用されます。
- **「バルーン画像色を変更しない」**（編集メニュー）: 有効にするとバルーン画像への色オーバーレイをスキップし、元の画像色のまま出力します。有効時はツールバーのカラー関連コントロールがすべて無効化されます。

---

### 3. プレビューで確認する

左カラムのリストからバルーンを選択すると中央のプレビューに表示されます。

- **balloonc系（c0〜c4）**: 入力ボックス（communicatebox）のみ表示
- **balloons・balloonk・balloonp\*def系**: サンプルテキスト・矢印・マーカー類・カウンタ等を表示

右ペインの文字色・背景色の設定直下には、現在の配色（バルーン色 vs 文字色）に基づいたコントラスト比が表示されます。WCAG2とAPCAの2種類の指標を同時に確認できます。

- **WCAG2**: 従来の比率ベース指標。**AA（4.5:1以上）** が一般的な合格基準、**AAA（7.0:1以上）** がより厳しい基準。
- **APCA Lc値 / Bronze**: より知覚特性に近い新世代指標。**Bronze Simple Mode** では **|Lc| ≥ 60** を合格（Bronze✓）とします。

**注意**: プレビューはベースウェアの描画と完全に一致するものではありません。フォントのレンダリングや細部の表示は実際の動作環境で異なる場合があります（詳しくは「[プレビューと実際の表示の違い](#プレビューと実際の表示の違い)」を参照）。

#### プレビューの拡大・縮小

プレビューの表示倍率を変更できます。細かい位置調整をするときや、画像全体を見渡したいときに使います。

操作方法は次の4通りです。

| 操作 | 説明 |
|------|------|
| プレビュー右下のボタン | `−` / `＋` で1段階ずつ、中央のリストから直接選択 |
| Ctrl + マウスホイール | プレビュー上で回すと1目盛りごとに1段階変化 |
| 表示メニュー → プレビュー倍率 | 一覧から選択 |
| Ctrl + `+` / `-` / `0` | 拡大／縮小／等倍（100%）に戻す |

倍率は **25% / 50% / 75% / 100% / 150% / 200% / 300% / 400%** の8段階です。

- 画像が表示領域に収まらない場合はスクロールバーが表示されます。
- 拡大・縮小した状態でも**位置編集（ドラッグ）は使えます**。拡大すると1ピクセル単位の調整がしやすくなります。
- 左下には元画像のピクセル寸法が表示されます（倍率を変えても表示は変わりません）。
- 倍率は**アプリを終了すると100%に戻ります**（保存されません）。

**注意**: これは編集画面での表示倍率を変えるだけの機能で、**出力される画像には一切影響しません**。またベースウェア上で拡大表示したときの見た目を再現するものでもありません（詳しくは「[プレビューと実際の表示の違い](#プレビューと実際の表示の違い)」を参照）。

---

### 4. 設定を編集する

右カラムのバルーン設定タブで各種設定を編集できます。

#### 基本情報

バルーン名・作者情報・更新URLなど。変更は `descript.txt` / `install.txt` に反映されます。

#### バルーン詳細設定

フォント・テキスト色・座標・スタイルなどを編集できます。項目は種類ごとのアコーディオン（折りたたみ）に分かれており、見出しをクリックすると開閉します。
変更はリアルタイムでプレビューに反映されます。

##### 選択肢・アンカーの「ラスタオペレーション」

選択肢（cursor）・アンカー（anchor）の各グループには **「ラスタオペレーション」** チェックボックスがあります。これは descript.txt の `blendmethod` 設定を切り替えるもので、強調表示の描画方式が変わります。

- **ON（`blendmethod,copypen`）**: 矩形の枠色（pen）・塗り色（brush）を個別に指定できます。文字色・枠色・塗り色の3つを編集します。
- **OFF（`blendmethod,none`）**: ラスタオペレーションを使わず、文字色と影色で表現します。文字色・影色・影スタイルを編集します。

`blendmethod` に `copypen` / `none` 以外の値（`mergepennot` など）が設定されている場合、このアプリでは描画を再現できないため、チェックボックスはグレーアウトされます（マウスを乗せると理由が表示されます）。

##### 縦書き（vertical）

設定パネル上部（アコーディオンの上）の **「縦書き」** チェックボックスで、descript.txt の `vertical,0/1`（SSP 2.8.80 以降の機能）を切り替えられます。

縦書きにすると座標指定の意味が一部変わります（`origin.x` が1列目の右端になる、折り返しが `wordwrappoint.y` になるなど）。仕様の詳細は [UKADOC の vertical の項](https://ssp.shillest.net/ukadoc/manual/descript_balloon.html#vertical_2c0_2f1:1) を参照してください。

このアプリでは縦書き ON のとき、UI の表示が次のように切り替わります。

- 矢印の表記が **「矢印(右)」「矢印(左)」** になります（縦書きでは `arrow0` が右スクロール、`arrow1` が左スクロールの意味になるため）。画像の向きは「[画像の回転・反転](#画像の回転反転)」で調整できます
- 「位置編集」の折り返し項目が **「折り返し Y」**（上下ドラッグ）に切り替わります
- 「SSTPメッセージ 終端X」（`sstpmessage.xr`）が **「SSTPメッセージ 終端Y」**（`sstpmessage.yb`）に切り替わります。SSP では横書きで `xr`、縦書きで `yb` だけが使われ、他方は無視されるためです
- カウンタのラベルが **「カウンタ 列X」「カウンタ 下端Y」** になります（横書きでは「右端X」「上端Y」）
- テキスト域オーバーレイが折り返しYの横線に切り替わります（折り返しYが未指定の場合はテキスト域の下端に表示されます）

プレビューのサンプルテキストも縦書きで描画されます。

- フォントは縦書き用異体（`@`付きフォント名）を使用します。指定フォントに縦書き用異体が無い場合（同梱の欧文フォントなど）は `@ＭＳ ゴシック` に自動で差し替わります（SSP と同様の挙動）
- 半角英数字は縦書きフォントの挙動どおり、右に90度回転して表示されます
- 選択肢の強調矩形（square）は縦長に、アンカーの下線は列の右側に描画されます
- SSTPメッセージは `sstpmessage.y` を開始位置（先頭文字の上端）として下方向へ描画されます。`sstpmessage.yb` は描画の終端（下限）で、そこに収まらない文字は表示されません。省略した場合はベース画像の下端までが描画範囲になります
- カウンタ数値は文字列末端（下端）が `number.y` に一致するように縦書きで描画されます
- 入力ボックス（balloonc系）は横書きのままです

横書きの場合も同様に、`sstpmessage.x` が開始位置、`sstpmessage.xr` が終端（右端）です。SSP は実際の送信元名を表示するためプレビューとは文字列の長さが異なりますが、終端の指定が狭すぎて文字が切れてしまう場合はプレビューでも同じように切れて見えます。

**終端座標に `0` を指定すると文字が表示されなくなります。** `0` は「指定なし」ではなく「左端（上端）で打ち切る」という意味になるためです。

設定パネルの「終端X」「終端Y」欄には、descript.txt に記述がないとき `-1`（ベース画像の右端・下端から 1px 内側）が初期値として表示されます。負値なのは、サイズの異なるバルーンが混在していても、それぞれの画像の端を基準に解決させるためです。この初期値は表示されているだけで、欄を操作しない限り descript.txt には書き込まれません。

#### 座標キーの別名

descript.txt では、次の座標キーを 2 通りの書き方で指定できます。どちらで書いても意味は同じです。

| 書き方 | 別の書き方 | 意味 |
| --- | --- | --- |
| `number.xr` | `number.x` | カウンタ数値のX座標。指定座標が数値の右端になります |
| `number.y` | `number.yb` | カウンタ数値のY座標。横書きでは上端、縦書きでは下端を指します |

左の列は古くから使われている書き方で、対応するベースウェアが多いのはこちらです。右の列は後から追加された別名で、起点を読み取りやすくするためのものです（`number.x` は左起点でも書けること、`number.yb` は下端を指すことを表しています）。

このアプリはどちらの表記でも読み込めます。書き戻すときは**素材にすでに書かれている方の表記を維持**し、どちらも無ければ左の列の書き方（`number.xr` / `number.y`）で追記します。

同じファイルに両方の表記があった場合は、片方に値を書き込み、もう片方はコメントアウトして「どちらが有効か分からない」状態を解消します。

#### 個別設定

設定パネル上部に「個別設定」コントロールがあります。

- **「追加」ボタン**: 選択バルーン専用の設定ファイル（例: `balloonk0s.txt`）を作成します。追加後は設定タブの変更がそのバルーン専用に書き込まれ、他のバルーンには影響しません。
- **「削除」ボタン**: 個別設定を破棄し、共通設定（`descript.txt`）に戻します。
- 個別設定ファイルは `descript.txt` からの差分のみを保持します。

#### 位置編集

テキスト域・各マーカー・入力ボックスなどのUI要素の座標を、プレビュー上でドラッグして編集できます。

- 編集したい要素のボタンをクリックするとドラッグ編集モードになり、プレビュー上にハンドルが表示されます。
- ハンドルをドラッグすると座標が更新されます。
- **テキスト域オーバーレイ表示**（表示メニュー）: 位置編集モード以外でもオーバーレイを常時表示できます。

**注意**: SSTPメッセージ・カウンタをドラッグ編集するときは、文字は表示されず十字のハンドルだけが出ます。位置の目安にはプレビュー本体の表示をご覧ください。

`origin.x` / `origin.y`（テキスト開始位置）を指定している場合、その位置に十字が表示されます。

開始位置がテキスト域からはみ出しているとき、その扱いは方向によって異なります。文字送りが始まる側（横書きなら左と上、縦書きなら右と上）へはみ出した指定はテキスト域の端に留められます。反対側（横書きなら右と下、縦書きなら左と下）へはみ出した指定はそのまま使われ、文字がテキスト域の外や画像の外に描かれます。このとき十字と破線が橙色になります。

たとえば縦書きで `origin.x,0` と指定すると、1列目の右端が画像の左端（x=0）になるため、文字が画像の外へ出て見えなくなります。`origin` は 0 を「未指定」ではなく座標として扱うためです。開始位置を指定する必要がなければ、`origin.x` / `origin.y` の行自体を書かずにテキスト域の定義へ任せてください。

---

### 5. 画像をプロジェクトに追加する

メニューバーの「ファイル > 画像をプロジェクトに追加...」で PNG 画像をインポートできます。プロジェクトを開いている状態でのみ実行できます。

- 画像をドラッグ＆ドロップしてインポートすることもできます。
- インポートダイアログでは、ファイル名から推測したカテゴリ（バルーン・矢印・マーカー等）やスコープ番号が自動で設定されます。必要に応じて変更してください。
- ダイアログ右側に取り込む画像のプレビューが表示されます（透過部分はチェッカー柄で表示）。
- 保存予定のファイル名が表示され、同名ファイルが既に存在する場合は上書き確認が出ます。
- `profile/slbe/files.txt` に既に定義済みのバルーン名をインポートすると、該当の定義行がコメントアウトされます（物理画像が優先されます）。

**注意**: 画像のインポートは「元に戻す（Ctrl+Z）」で取り消せません。

#### 画像の回転・反転

左ペインの「画像一覧」から画像を選んで単体プレビューすると、右ペインに「画像編集」セクションが表示され、**左90°回転／右90°回転／180°回転／左右反転／上下反転**ができます。縦書きバルーン用に矢印画像の向きを変える場合などにご利用ください。

- 90度単位の回転・反転のため、画質の劣化はありません。
- 操作すると**ファイルが直接書き換わります**。「元に戻す（Ctrl+Z）」の対象外です（逆の操作で元に戻せます）。
- 同名の `.pna` ファイルがある画像は、`.png` と `.pna` の両方が一緒に変換されます（ペア形式は維持されます）。
- `.pna` の無い 24bit PNG（tRNS 透過・カラーキー透過）は、変換時に 32bit PNG（アルファ付き）へ正規化されます。出力時には `use_self_alpha,1` が書き込まれるため、SSP 上の表示には影響しません。

---

### 6. 保存する

メニューバーの「ファイル > 上書き保存（Ctrl+S）」で保存します。
以下のファイルが書き出されます。

- `descript.txt` / `install.txt`
- 個別設定ファイル（`balloonk0s.txt` など、差分がある場合のみ）
- `profile/slbe/profile.json`（配色設定）

未保存の変更がある状態でプロジェクトを切り替えたり終了しようとすると、確認ダイアログが表示されます。

**注意**: **「元に戻す（Ctrl+Z）」で戻せる操作はテキスト・色設定の変更のみです。画像のインポート・画像の回転・反転は取り消せません**（回転・反転は逆向きの操作で元に戻せます）。

---

### 7. バルーンとして出力する

メニューバーの「ファイル > バルーンとして出力（Ctrl+E）」をクリックします。
`output/（インストール先フォルダ名）/` に以下のファイルが出力されます。

- バルーン画像（`balloonk0〜`、`balloons0〜`、`balloonp*def0〜`、`balloonc0〜4` など）
- パーツ画像（`arrow0/1`、`clickwait`、`online0〜`、`sstp`、`marker` など）
- `descript.txt` / `install.txt`
- 説明テキスト（`readme.txt` など、後述）
- 個別設定ファイル（差分が存在するバルーンのみ）

出力が完了するとダイアログが表示されます。「フォルダを開く」を押すと出力先フォルダをエクスプローラで開きます。

**説明テキストについて**: 説明テキストは `descript.txt` の `readme,ファイル名` で指定されたファイル名（指定が無ければ `readme.txt`）で、プロジェクト内の実ファイルをそのまま出力します。アプリは説明テキストの中身や文字コード（`readme.charset`）を編集しません。

**注意**: 出力される `descript.txt` では `use_self_alpha`・`use_input_alpha`・`overlay_outside_balloon` が強制的に `1` で書き出されます。このアプリはPNGのアルファチャンネルを使って画像合成を行うため、これらを `0` にした場合の動作は保証されません。

---

## メニュー

### ファイルメニュー

| 項目 | 説明 |
|------|------|
| 新規プロジェクト... | 空のプロジェクトを新規作成します |
| プロジェクトを開く... | 既存プロジェクトを一覧から選択して開きます |
| フォルダからプロジェクトを作成... | 既存の素材フォルダを取り込んでプロジェクトを作成します |
| 上書き保存 (Ctrl+S) | 現在の編集内容をプロジェクトフォルダに保存します |
| 別名で保存... | 現在のプロジェクトを別名でコピー保存します |
| 画像をプロジェクトに追加... | PNG画像をプロジェクトにインポートします |
| プロジェクトをエクスプローラで開く | プロジェクトフォルダをエクスプローラで開きます |
| バルーンとして出力 (Ctrl+E) | バルーン画像・設定ファイルを output/ に出力します |

### 編集メニュー

| 項目 | 説明 |
|------|------|
| 元に戻す (Ctrl+Z) | 直前の操作を取り消します（最大20件）。画像インポートは取り消せません |
| やり直し (Ctrl+Y) | 取り消した操作をやり直します |
| バルーン設定を初期値に戻す | 配色・descript設定・個別設定をデフォルト値にリセットします |
| レイアウト定義を編集... | `profile/slbe/files.txt`（レイヤー合成定義）を GUI で編集します |
| バルーン画像色を変更しない | 有効にするとバルーン画像への色オーバーレイを無効化します |
| 奇数・偶数番バルーンを自動補完 | 有効にすると対応する偶数/奇数バルーンを反転して自動生成します |

### 表示メニュー

| 項目 | 説明 |
|------|------|
| プレビュー更新 (F5) | プレビューを強制再描画します |
| ウィンドウレイアウトをリセット | ペインの幅をデフォルトに戻します |
| テキスト域オーバーレイ表示 | テキスト域・折り返し位置のオーバーレイを常時表示します |
| アプリテーマ | アプリのライト／ダークテーマを切り替えます |
| プレビューテキスト | 通常サンプル／折り返し幅確認テキストを切り替えます |
| プレビュー背景 | プレビューキャンバスの背景をチェッカー（透明）または単色から選択します |
| プレビュー倍率 | プレビューの表示倍率を8段階から選択します |

---

## profile/slbe/files.txt について

`profile/slbe/files.txt` はバルーン画像の合成レイアウトを定義するファイルです。
このファイルに**有効なバルーン定義行**（`バルーン名,...` の行。コメント行 `//` や空行を除く）があると、アプリはその定義に従って複数のレイヤー画像を合成し、配色を適用します。
定義行が一つも無い場合（ファイルが無い、または中身がコメント・空行のみの場合）は、合成・配色を行わず、プロジェクト内の `balloon*.png` をそのまま使います。

```
// コメント行（先頭が // の行は無視される）
balloonc0,c_edge.png:edge,c_base.png:base,c0_text.png:text
balloonk0,k0_edge.png:edge,k0_base.png:base
```

- 書式: `バルーン名,ファイル名:色種別,...`（奥側レイヤーから順に記述）
- 色種別: `edge`（縁取り色）/ `base`（塗り色）/ `text`（テキスト枠色）/ `none`（塗り替えなし）
- `balloonk1/k3`・`balloons1/s3` などは記述を省略すると、対応する偶数バルーンを左右反転して自動生成します（「奇数・偶数番バルーンを自動補完」が有効な場合）。
- ファイルが存在しない・必須バルーンが未定義など、記述に問題がある場合は起動時にエラーが表示されます。

### GUI で編集する

メニューバーの「編集 > レイアウト定義を編集...」で、`profile/slbe/files.txt` を専用ウィンドウで編集できます。

- バルーンごとに、合成するレイヤー（ファイル名と色種別）を追加・削除・並べ替えできます。
- 各バルーンの種別（通常系 `balloons*` / 相方系 `balloonk*` など、入力ボックス `balloonc*`）を切り替えられます。
- コメント行の追加・一括削除、バルーン順への並べ替えができます。
- 「適用」すると変更がプレビューに反映され、保存時に `profile/slbe/files.txt` へ書き出されます。空のコメント行は書き込み時に空行として出力されます。

### 旧形式からの自動移行

素材フォルダ直下の `files.txt`（旧形式）が存在し、このアプリのレイアウト定義として解釈できる場合は、`profile/slbe/files.txt` へ自動的に移行します。

---

## レイアウト定義が無い場合の動作

レイアウト定義（`profile/slbe/files.txt` の有効なバルーン定義行）が一つも無いプロジェクトでは、レイヤー合成・配色を行わず、`balloon*.png` ファイルをそのままプレビュー・出力します（バルーン画像への色オーバーレイは行われません）。

※ レイアウト定義はメニューバーの「編集 > レイアウト定義を編集...」からも作成・編集できます。`files.txt` を直接編集する必要はありません。

---

## プロジェクトのフォルダ構成

```
projects/
  （プロジェクト名）/
    descript.txt          … バルーン設定（テキスト正本）
    install.txt           … インストール設定（テキスト正本）
    readme.txt            … 説明文（任意）
    balloon*.png          … バルーン・パーツ画像
    balloon*s.txt         … 個別バルーン設定（差分のみ、任意）
    profile/
      slbe/
        files.txt         … レイヤー合成定義（任意、定義行が無ければ画像をそのまま使用）
        profile.json      … 配色設定（このアプリ固有）
```

`profile/slbe/` 以下はこのアプリ固有のファイルです。SSPには配布不要です（出力時には含まれません）。

---

## プレビューと実際の表示の違い

プレビューはベースウェア上での表示を再現するものではありません。以下の点で実際の表示と異なります。

### フォントの描画

**アウトラインフォント**（メイリオ、游ゴシックなど一般的なフォント）では、プレビューと SSP とで**文字の線の太さや輪郭のなめらかさがわずかに異なります**。

**ビットマップフォント**（ＭＳ ゴシックなど）では、この差は生じません。

文字の見え方まで正確に確認したい場合は、出力して実際のベースウェア上でご確認ください。

### 拡大・縮小

このアプリのプレビュー拡大・縮小は、**編集作業のための表示機能**です。

このアプリの倍率は **8段階の固定値**で、拡大には**最近傍法**（ドットをそのまま四角く引き伸ばす方法）を使っています。1ピクセル単位の位置調整をしやすくするためで、補間をかけるとドットの境界がぼやけ、座標を正確に合わせづらくなるためです。

一方ベースウェア側の拡大・縮小は、**実装状況・倍率の指定方法・拡縮時の補間処理のいずれもベースウェアによって異なる可能性があります**。

そのため、**拡大したプレビューの見た目は、ベースウェア上で拡大表示したときの見た目とは一致しません**。バルーンが実際にどう表示されるかを確認したい場合は、100% 表示で判断するか、出力して実際のベースウェア上で確認してください。

なお、この機能は表示倍率を変えるだけで、**出力される画像のサイズや内容には一切影響しません**。

---

## このアプリが扱わない項目について

このアプリは「テキスト正本方式」を採用しています。`descript.txt` などのテキストファイルは読み込んだ内容を**そのまま保持**し、編集 UI で扱う項目だけを書き換えて保存・出力します。

そのため、**このアプリの編集画面に無い項目も、`descript.txt` に書かれていればそのまま保持・出力されます**。下記の項目はプレビューや編集 UI には反映されませんが、記述が消えたり壊れたりすることはありません。外部エディタで自由に追記できます。

### 編集 UI には無いが、記述は保持される項目

下記は編集画面に項目がありませんが、`descript.txt` に書かれていれば**そのまま保持・出力**されます。外部エディタで追記できます。

- `id`、`dpi`
- `cursor` / `mousecursor` / `mousecursor.text` / `mousecursor.wait` / `mousecursor.arrow`（各種カーソル画像）
- `font.bold` / `font.italic` / `font.underline` / `font.strike` / `font.outline`（文字装飾。本アプリは文字色のみ対応）
- `disable.font.*`（「操作無効の文字」は**文字色のみ対応**。フォント名・サイズなどそれ以外の指定は保持のみ）
- `paint_transparent_region_black`
- `windowposition.x` / `windowposition.y` / `windowposition.limit`
- `recommended.ghost` / `recommended.ghost.path`
- パーツ画像のファイル名カスタム指定（`arrow.filename` / `marker.filename` / `clickwaitmarker.filename` / `onlinemarker.filename` / `sstpmarker.filename` など。個別設定ファイル `balloon*s.txt` の仕様）

これらの値はプレビューには反映されませんが、記述が消えたり壊れたりすることはありません。

### 出力時に固定値で書き込まれる項目

- `use_self_alpha` / `use_input_alpha` / `overlay_outside_balloon` は、出力時に **`1` で書き込まれます**。

### オンライン表示画像のアニメーション

- オンライン表示画像（`online*.png`）のアニメーションには対応していません。プレビューでは先頭のコマを静止表示します。

### 画像形式

- 読み込み・プレビュー・編集に対応する画像形式は **PNG（および `.pna` / `.pnr`）のみ**です。その他の形式の対応予定はありません。

### readme

- `readme.txt`（または `descript.txt` の `readme,ファイル名` で指定されたファイル）は、**中身を編集せずそのまま出力にコピー**します。文章の編集は外部エディタで行ってください。

### 座標のマイナス 2 つ表記

なお、右端・下端を基準にする座標（`origin.x` や `number.x` など）にマイナス値を指定するのは仕様どおりの用法です（ベース画像の右端・下端からのオフセットになります）。ここで説明するのは「マイナスを 2 つ重ねる」表記についてです。

UKADOC の仕様では、座標値にマイナスを指定すると**ベース画像の右下からの相対座標**として扱われます。そのうえで「どうしても左上基点のマイナス座標を指定したい場合は、`--数値` とマイナスを 2 つ重ねる」という表記が定められています。

**このアプリはマイナス 2 つ表記に対応していません。**

`--10` のように書かれた値は、プレビューでは **`0` として読み替えたうえで右下基点**として扱われるため、**右下端そのもの**の位置に表示されます。本来意図される「左上から -10」とは異なる位置になります。

ただしテキスト正本方式のため、**その項目を編集画面で操作しない限り、`descript.txt` の `--10` という記述はそのまま保持・出力されます**。SSP 上での表示は仕様どおりになります。

一方で、**該当する項目を編集画面や位置編集（ドラッグ）で操作すると、入力した値で上書きされ、マイナス 2 つ表記は失われます**。編集画面の数値欄には `0` と表示されるため、意図せず書き換えてしまわないようご注意ください。

まとめると、マイナス 2 つ表記を使っている項目については

- プレビューの表示位置が実際と食い違う
- その項目を編集すると表記が失われる

の 2 点にご注意ください。
